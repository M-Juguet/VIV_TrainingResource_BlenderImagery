import 'package:flutter/material.dart';

class ProgramCreatorScreen extends StatelessWidget {
  const ProgramCreatorScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Création de programme"));
  }
}
